{
  "name": "lumisxh_master",
  "version": "0.0.0",
  "author": "LuMiSxh",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build --parallel",
    "dev": "turbo run dev --parallel",
    "cleanup": "turbo run cleanup --parallel"
  },
  "devDependencies": {
    "eslint-config-custom": "workspace:*",
    "prettier": "^2.8.3",
    "turbo": "^1.7.0"
  },
  "engines": {
    "node": ">=15.0.0"
  },
  "packageManager": "pnpm@7.18.2"
}